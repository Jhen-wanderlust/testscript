


//Account Type

var listData = [
	'SA',
	'CA',
	'CC'
	
	]
	let genData = listData[Math.floor(Math.random() * listData.length)]
	var newdata = document.getElementById("txt1").value = genData;

  //Allowance
  var listData = [
    'LOAD ALLOWANCE',
    'MEAL ALLOWANCE',
    'INTERNET ALLOWANCE',
    'OB ALLOWANCE'
    ]
    let genData = listData[Math.floor(Math.random() * listData.length)]
    var newdata = document.getElementById("txtdesc").value = genData;

    //Bank Account
  var listData = [
    'BDO',
    'CBS',
    'SBA',
    'BPI',
    'RB'
    ]
    let genData = listData[Math.floor(Math.random() * listData.length)]
    var newdata = document.getElementById("modalField_bankdescription").value = genData;
  
  //Department
  var listData = [
    'MIS',
    'CSR',
    'HR',
    'SPE',
    'ACC'
    ]


    let genData = listData[Math.floor(Math.random() * listData.length)]
    var newdata = document.getElementById("modalField_deptdescription").value = genData;

   //Employee Status
  var listData = [
    'Provisionary',
    'Regular',
    'Trainee'
    
    ]
    let genData = listData[Math.floor(Math.random() * listData.length)]
    var newdata = document.getElementById("txt1").value = genData;
  //Employment Type
  var listData = [
    'Full-time',
    'part-time',
    'Casual',
    'contract',
    'piece rate employees',
    'Fixed term'
    ]
    let genData = listData[Math.floor(Math.random() * listData.length)]
    var newdata = document.getElementById("txt1").value = genData;

    //Leaves
  var listData = [
    'parental Leave',
    'compassionate Leave',
    ' sick Leave',
    'personal Leave',
    'annual Leave'
    ]
    let genData = listData[Math.floor(Math.random() * listData.length)]
    var newdata = document.getElementById("txt1").value = genData;
    
    var listData = [
      'BAS',
      'BEN',
      'DM',
      'NT',
      'REG',
      'SUP'
      ]
      let genData = listData[Math.floor(Math.random() * listData.length)]
      var newdata = document.getElementById("txtclass").value = genData;
    //Loans
  var listData = [
    'COMPANY LOAN',
    'SSS LOAN',
    'PAG-IBIG LOAN',
    'GOVERNMENT LOAN'
    ]
    let genData = listData[Math.floor(Math.random() * listData.length)]
    var newdata = document.getElementById("txt1").value = genData;

     //Other Deductions
  var listData = [
    'SALARY ADJUSTMENT',
    'OVERPAY',
    'DAMAGES LOSS',
    'CASH ADVANCE'
    ]
    let genData = listData[Math.floor(Math.random() * listData.length)]
    var newdata = document.getElementById("txtdesc").value = genData;

    document.getElementById("txtclass").value = NT;
    document.getElementById("txtclass").value = TD;



  //Other Earnings
  var listData = [
    'TRANSPO ALLOWANCE',
    'CASH ADVANCE',
    'OTHER TAXABLE INCOME',
    'RICE SUBSIDY',
    'LAUNDRY ALLOWANCE',
    'MONTHLY COMMISION'
    ]
    let genData = listData[Math.floor(Math.random() * listData.length)]
    var newdata = document.getElementById("txtdesc").value = genData;


    //tax classs
    var listData = [
      'BAS',
      'BEN',
      'NT',
      'NT',
      'REG',
      'SUP'
      ]
      let genData = listData[Math.floor(Math.random() * listData.length)]
      var newdata = document.getElementById("txtclass").value = genData;


    //Payroll Group
  var listData = [
    'Weekly',
    'Semi-Monthly',
    'Monthly'
    
    ]
    let genData = listData[Math.floor(Math.random() * listData.length)]
    var newdata = document.getElementById("txt_paygroup").value = genData;
    //sel_paycycle
    var newdata = document.getElementById("sel_paycycle").value = genData;
    
    

    //Piece Rate
  var listData = [
    'Box-1',
    'Scrap-1',
    'Box-2',
    'Scrap-2',
    'Box-3',
    'Scrap-3'
    
    ]
    let genData = listData[Math.floor(Math.random() * listData.length)]
    var newdata = document.getElementById("txtdesc").value = genData;

//amount
  var NumArray = [
    '20,000',
    '50,000',
    '30,000',
    '70,000',
    '60,000',
    '90,000',
    '30,000'
  ];
  var randomNumber = NumArray[Math.floor(Math.random()*NumArray.length)]
  var newdata = document.getElementById("txtamount").value = randomNumber;

  // TaxClass
  var listData = [
    'BAS',
    'BEN',
    'NT',
    'NT',
    'REG',
    'SUP'
    ]
    let genData = listData[Math.floor(Math.random() * listData.length)]
    var newdata = document.getElementById("txtclass").value = genData;

    //Position
  var listData = [
    'Executive Officer',
    'Branch Manager',
    'Supervisor',
    'IT Manager',
    'Operation Manager',
    'Assistant Supervisor',
    'IT Staff'
    ]
    let genData = listData[Math.floor(Math.random() * listData.length)]
    var newdata = document.getElementById("txt1").value = genData;

    //Branch
  var listData = [
    'CALOOCAN',
    'MALABON',
    'NAVOTAS',
    'VALENZUELA'
    ]
    let genData = listData[Math.floor(Math.random() * listData.length)]
    var newdata = document.getElementById("txt1").value = genData;
    //Division
    var listData = [
      'PRODUCTION',
      'CHECKING',
      'RELEASING',
      'PREPARATION'
      ]
      let genData = listData[Math.floor(Math.random() * listData.length)]
      var newdata = document.getElementById("txt1").value = genData;
  
   //I.D. Type
   var listData = [
    'DRIVERS LICENSE',
    'SSS',
    'TIN',
    'PHILHEALTH'
    ]
    let genData = listData[Math.floor(Math.random() * listData.length)]
    var newdata = document.getElementById("txt1").value = genData;

    //Industry
  var listData = [
    'BUSINESS',
    'SOFTWARE',
    'HARDWARE',
    'CORPORATE'
    ]
    let genData = listData[Math.floor(Math.random() * listData.length)]
    var newdata = document.getElementById("txt1").value = genData;

    //Job / Rank Level
  var listData = [
    'EXECUTIVE LEVEL 1',
    'EXECUTIVE LEVEL 2',
    'MANAGER LEVEL 1',
    'MANAGER LEVEL 2'
    ]
    let genData = listData[Math.floor(Math.random() * listData.length)]
    var newdata = document.getElementById("txt1").value = genData;
    //Nationality
    var listData = [
      'FILIPINO',
      'CHINESE',
      'JAPANESE',
      'KOREAN'
      ]
      let genData = listData[Math.floor(Math.random() * listData.length)]
      var newdata = document.getElementById("txt1").value = genData;
      
      //Religion
    var listData = [
      'CATHOLIC',
      'IGLESIA',
      'CHRISTIAN',
      'BORN AGAIN'
      ]
      let genData = listData[Math.floor(Math.random() * listData.length)]
      var newdata = document.getElementById("txt1").value = genData;

      //Suffix
    var listData = [
      'JR.',
      'III',
      'IV',
      'N/A'
      ]
      let genData = listData[Math.floor(Math.random() * listData.length)]
      var newdata = document.getElementById("txt1").value = genData;


       //Employee
    var listData = [
      'Employee Certification',
      'Employee Default AllowanceI',
      'Employee Default Deductions',
      'Employee Default Earnings'
      ]
      let genData = listData[Math.floor(Math.random() * listData.length)]
      var newdata = document.getElementById("txt1").value = genData;
      
      localStorage.setItem("getRandom", 0);

      let getRand = localStorage.getItem("getRandom");
  
  
      
// Array to store indexes which are left to access. 
// It helps in accessing values without repeating


var alreadyDone = [];

// Function picking random values from array
var randomValueFromArray = (myArray) => {
// If alreadyDone is empty then fill it will indexes equal
// to the size of myArray
if (alreadyDone.length === 0) {
for (var i = 0; i < myArray.length; i++) alreadyDone.push(i);
}

// Generate random number within the range of 
// length of alreadyDone array
var randomValueIndex = Math.floor(Math.random() * alreadyDone.length);

// Getting unaccessed index of myArray using above 
// random number
var indexOfItemInMyArray = alreadyDone[randomValueIndex];

// remove this index from alreadyDone array because
// we are accessing it now.
alreadyDone.splice(randomValueIndex, 1);

// Get the value
localStorage.setItem("getRandom", parseInt(getRand) + 1);
return myArray[indexOfItemInMyArray];
};

var gendata = randomValueFromArray(["a", "b", "c", "d", "e", "f"])
var newdata = document.getElementById("modalField_religiondesc").value = gendata([0, 1, 2, 3,4, 4]);

  

  
  
  
  
  
  
  
  
  
  